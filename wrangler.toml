name = "roadbook-studio"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# Cloudflare Pages configuration
# Deploy with: npx wrangler pages deploy . --project-name roadbook-studio

# Workers AI binding for /api/parse
[[ai_bindings]]
binding = "AI"
